doctype html
html
	include ./head.pug
	body
		include ./header.pug
		include ./navbar.pug
		div#body
			a(href='/form') Form test
			br
			a(href='/react') React test
			br
			if(unlocked)
				p You unlocked another page!
				a(href=url) URL Shortener
			else
				p Page Views: #{visits}
		script(src="/static/js/root.js")
		include ./footer.pug